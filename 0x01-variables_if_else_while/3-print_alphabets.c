#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= '2') {
			putchar(c);
			c++;
		}
	while
		(d <= '2') {
			putchar (d);
			d++;

		}
	putchar('\n');
	return (0);
}
